# eval/

Eval + observer harness (seeded empty). The product test suite lives in `src/__tests__/` (58 Vitest cases). Add end-to-end / behavioral evals and the observer here as the harness self-improves. See `brain/MOC - agentdial.md`.
