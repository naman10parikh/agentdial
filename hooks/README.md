# hooks/

Project-local hook extension point (seeded empty). The active inherited hooks live under `.claude/hooks/` (wired via `.claude/hooks/hook-config.json`). Add agentdial-specific hooks here. See `brain/MOC - agentdial.md`.
