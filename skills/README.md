# skills/

Project-local skill extension point (seeded empty). The active inherited skill set lives under `.claude/skills/`. Add agentdial-specific skills here as one file per skill. See `brain/MOC - agentdial.md`.
