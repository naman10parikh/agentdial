# tools/

Project-local tool extension point (seeded empty). Maintenance scripts live in `scripts/`; the agent-facing MCP server is `agentdial mcp-serve` (`src/commands/mcp-serve.ts`). Add standalone tooling here. See `brain/MOC - agentdial.md`.
